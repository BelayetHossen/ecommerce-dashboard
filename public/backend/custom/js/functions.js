(function ($) {
    $(document).ready(function () {

        // empty function
        function empty(msg, type) {
            return `<p class="text-${type}">${msg}</p>`;
        }



        // add new permission




















        //
    })

})(jQuery)
